package Model;

import java.util.List;

public class Camels extends PackAnimals{

    public Camels(String name, float weight, List<String> execCommands, String birthDate) {
        super(name, weight, execCommands, birthDate);
    }
    
}
