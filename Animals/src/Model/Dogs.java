package Model;

import java.util.List;

public class Dogs extends Pets{

    public Dogs(String name, float weight, List<String> execCommands, String birthDate) {
        super(name, weight, execCommands, birthDate);
    }
    
}
